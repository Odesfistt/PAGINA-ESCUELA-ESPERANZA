---
import { talleres } from "../../data/siteData";
import AccionesPaginaInterna from "./AccionesPaginaInterna.astro";
---

<main class="internal-page">
  <section class="internal-hero">
    <div class="internal-hero-content">
      <span class="internal-kicker">Actividades escolares</span>
      <h1>Talleres</h1>
      <AccionesPaginaInterna sectionHref="/" sectionLabel="Inicio" sectionIcon="fa-house" />
    </div>
  </section>

  <section class="about-page-section">
    <div class="about-page-detail about-page-detail--compact">
      <section class="workshops-intro-panel">
        <div>
          <span>Talleres escolares</span>
          <h2>Actividades disponibles</h2>
        </div>
        <p>Espacios de aprendizaje, participación y desarrollo integral para estudiantes de primer y segundo ciclo básico.</p>
      </section>

      <div class="workshop-grid">
        {talleres.map((taller) => (
          <a href={taller.href} class="workshop-card workshop-card--link">
            <img src={taller.imagen} alt={taller.titulo} />
            {taller.ciclo && <span class="workshop-card-cycle">{taller.ciclo}</span>}
            <h2>{taller.titulo}</h2>
            <p>{taller.descripcion[0]}</p>
            <span class="workshop-card-action">
              {"sinInformacion" in taller && taller.sinInformacion ? "Contacto" : "Ver taller"}
              <i class={`fa-solid ${"sinInformacion" in taller && taller.sinInformacion ? "fa-envelope" : "fa-arrow-right"}`}></i>
            </span>
          </a>
        ))}
      </div>
    </div>
  </section>
</main>
